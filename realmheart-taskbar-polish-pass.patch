diff --git a/src/ui/bar/widgets/BarIconButton.cpp b/src/ui/bar/widgets/BarIconButton.cpp
index 7809a34..ab5d90d 100644
--- a/src/ui/bar/widgets/BarIconButton.cpp
+++ b/src/ui/bar/widgets/BarIconButton.cpp
@@ -17,7 +17,7 @@ BarIconButton::BarIconButton(
     overlay_ = gtk_overlay_new();
     stack_ = gtk_stack_new();
     icon_ = std::make_unique<ThemedSvgIcon>();
-    icon_->set_size(26);
+    icon_->set_size(24);
 
     fallback_ = gtk_label_new(nullptr);
     gtk_widget_add_css_class(fallback_, "realmheart-bar-fallback-icon");
diff --git a/src/ui/bar/widgets/SystemMonitorWidget.cpp b/src/ui/bar/widgets/SystemMonitorWidget.cpp
index e0e1a87..979dc63 100644
--- a/src/ui/bar/widgets/SystemMonitorWidget.cpp
+++ b/src/ui/bar/widgets/SystemMonitorWidget.cpp
@@ -82,7 +82,7 @@ SystemMonitorWidget::SystemMonitorWidget(
     };
     metric_icons_.reserve(metric_specs.size());
     for (const auto& [path, css_class] : metric_specs) {
-        auto icon = std::make_unique<ThemedSvgIcon>(path, 22);
+        auto icon = std::make_unique<ThemedSvgIcon>(path, 20);
         icon->add_css_class("realmheart-system-monitor-metric");
         icon->add_css_class(css_class);
         gtk_box_append(GTK_BOX(metrics), icon->widget());
diff --git a/src/ui/bar/widgets/ThemedSvgIcon.cpp b/src/ui/bar/widgets/ThemedSvgIcon.cpp
index 6a2afe4..6addf6b 100644
--- a/src/ui/bar/widgets/ThemedSvgIcon.cpp
+++ b/src/ui/bar/widgets/ThemedSvgIcon.cpp
@@ -62,6 +62,17 @@ std::string resolve_tokens(std::string source, const ColorKey& colors) {
     replace_all(source, "var(--rh-icon-primary, currentColor)", primary);
     replace_all(source, "var(--rh-icon-accent, #ffd66b)", accent);
     replace_all(source, "var(--rh-icon-on-accent, #18151f)", on_accent);
+    replace_all(source, "var(--rh-icon-primary)", primary);
+    replace_all(source, "var(--rh-icon-accent)", accent);
+    replace_all(source, "var(--rh-icon-on-accent)", on_accent);
+
+    const std::string injected_style =
+        "<style>:root{--rh-icon-primary:" + primary +
+        ";--rh-icon-accent:" + accent +
+        ";--rh-icon-on-accent:" + on_accent + ";}</style>";
+    const std::size_t svg_open = source.find('>');
+    if (svg_open != std::string::npos) source.insert(svg_open + 1, injected_style);
+
     replace_all(source, "currentColor", primary);
     return source;
 }
diff --git a/styles/taskbar/bar.css b/styles/taskbar/bar.css
index 43368f3..f41c494 100644
--- a/styles/taskbar/bar.css
+++ b/styles/taskbar/bar.css
@@ -25,18 +25,20 @@ window.realmheart-vertical-bar-window,
 .realmheart-vertical-bar {
   background-color: transparent;
   color: @rh_text;
-  padding: 13px 5px 14px 5px;
+  padding: 13px 4px 14px 6px;
 }
 
 /* Top zone: the three controls are intentionally spread across the upper rail. */
 .realmheart-bar-top-cluster {
   border-spacing: 0;
   margin-top: 7px;
+  margin-left: 2px;
 }
 
 /* Workspace zone remains the visual centre of the rail. */
 .realmheart-workspace-section {
   border-spacing: 9px;
+  margin-left: 2px;
 }
 
 .realmheart-bar-separator {
@@ -55,6 +57,7 @@ window.realmheart-vertical-bar-window,
 .realmheart-bar-bottom-cluster {
   border-spacing: 0;
   margin-bottom: 0;
+  margin-left: 2px;
 }
 
 .realmheart-bar-clock {
diff --git a/styles/taskbar/icons.css b/styles/taskbar/icons.css
index 306db44..c58335a 100644
--- a/styles/taskbar/icons.css
+++ b/styles/taskbar/icons.css
@@ -1,26 +1,24 @@
 /* Theme-token SVG taskbar icons and their button surfaces. */
 
 .realmheart-bar-icon-button {
-  min-width: 40px;
-  min-height: 40px;
-  padding: 3px;
+  min-width: 34px;
+  min-height: 34px;
+  padding: 2px;
   margin: 0;
   color: @rh_text;
-  background-color: alpha(@rh_surface, 0.24);
-  border: 1px solid alpha(@rh_primary, 0.90);
+  background-color: transparent;
+  border: none;
   border-radius: 13px;
   box-shadow: none;
 }
 
 .realmheart-bar-icon-button:hover {
-  background-color: alpha(@rh_surface_variant, 0.52);
-  border-color: @rh_primary;
+  background-color: alpha(@rh_surface_variant, 0.20);
 }
 
 .realmheart-bar-icon-button:active,
 .realmheart-bar-icon-button:checked {
-  background-color: alpha(@rh_primary, 0.20);
-  border-color: @rh_primary;
+  background-color: alpha(@rh_primary, 0.16);
 }
 
 .realmheart-bar-svg-icon {
@@ -62,30 +60,30 @@
 
 /* Upper-zone placement, matching the deliberately spacious layout sketch. */
 .realmheart-bar-icon-button.realmheart-media-button {
-  margin-top: 108px;
+  margin-top: 96px;
 }
 
 .realmheart-system-monitor-pill {
   min-width: 40px;
   min-height: 118px;
   padding: 7px 4px;
-  margin-top: 66px;
+  margin-top: 54px;
   color: @rh_text;
-  background-color: alpha(@rh_surface, 0.24);
-  border: 1px solid alpha(@rh_primary, 0.90);
+  background-color: alpha(@rh_surface, 0.14);
+  border: 1px solid alpha(@rh_primary, 0.42);
   border-radius: 19px;
   box-shadow: none;
 }
 
 .realmheart-system-monitor-pill:hover {
-  background-color: alpha(@rh_surface_variant, 0.52);
-  border-color: @rh_primary;
+  background-color: alpha(@rh_surface_variant, 0.24);
+  border-color: alpha(@rh_primary, 0.58);
 }
 
 .realmheart-system-monitor-pill:active,
 .realmheart-system-monitor-pill:checked {
-  background-color: alpha(@rh_primary, 0.20);
-  border-color: @rh_primary;
+  background-color: alpha(@rh_primary, 0.12);
+  border-color: alpha(@rh_primary, 0.66);
 }
 
 .realmheart-system-monitor-metrics {
diff --git a/styles/taskbar/workspaces.css b/styles/taskbar/workspaces.css
index cc59afa..2707fba 100644
--- a/styles/taskbar/workspaces.css
+++ b/styles/taskbar/workspaces.css
@@ -3,7 +3,7 @@
 .realmheart-workspace-stack {
   border-spacing: 5px;
   margin: 0;
-  padding: 12px 2px;
+  padding: 12px 0;
   background-color: alpha(@rh_surface, 0.18);
   border: 1px solid alpha(@rh_outline, 0.20);
   border-radius: 25px;
