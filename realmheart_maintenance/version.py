"""Release identity for standalone Python maintenance tools.

Kept independent of manifest loading and host/runtime health. The release
contract tests compare this value with the canonical product manifest/CMake.
"""

RELEASE_VERSION = "0.7.8"
