#pragma once

// Kept as a compatibility include for unfinished branches. Realmheart now uses
// one display-wide ThemeStyles provider instead of one provider per widget.
#include "ui/components/BaseWidget.hpp"
